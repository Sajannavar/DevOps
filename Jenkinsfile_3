pipeline{
    agent any

    stages{
        stage('Scrit Usage'){
            steps{
                script{
                    def age = 5
                    def result = age * 2
                    echo "Hello my age is ${result}"
                }
            }
        }
    }
}